apiVersion: v1
kind: ConfigMap
metadata:
  name: {{.Values.configMaps.name.nginx_config}}
data:
  {{- $files := .Files }}
  {{- range tuple "../configmap/nginx.conf" }}
  {{ . }}: "{{ range $files.Lines . }}{{.}}\n{{ end }}"
  {{- end}}