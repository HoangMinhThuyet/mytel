apiVersion: v1
kind: ConfigMap
metadata:
  name: {{.Values.configMaps.name.fpm_pool_config}}
data:
  {{- $files := .Files }}
  {{- range tuple "../configmap/fpm-pool.conf" }}
  {{ . }}: "{{ range $files.Lines . }}{{.}}\n{{ end }}"
  {{- end}}