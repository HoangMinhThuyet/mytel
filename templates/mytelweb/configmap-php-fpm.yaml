apiVersion: v1
kind: ConfigMap
metadata:
  name: {{.Values.configMaps.name.com_configmap}}
data:
  {{- $files := .Files }}
  {{- range tuple "../configmap/fpm-pool.conf" }}
  {{ . }}: "{{ range $files.Lines . }}{{.}}\n{{ end }}"
  {{- end}}